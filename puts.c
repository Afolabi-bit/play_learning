#include <stdio.h>

/**
 * main - Print text
 * Description: More text
 * Return: Zero
 */
int trial();
int main()
{	trial(12, 100);
	return (0);
}

int trial(int a, int b)
{
	printf("%d\n", a + b);
}
